UserUpdateManyAndReturnArgs:
  type: object
  properties:
    data:
      oneOf:
        - $ref: '#/definitions/test/UserUpdateInput'
        - $ref: '#/definitions/test/UserUpdateInputUnchecked'
    where:
      $ref: '#/definitions/test/UserWhereInput'
      description: Filter for update
    select:
      $ref: '#/definitions/test/UserSelectOmitFields'
      description: Fields to select
    omit:
      $ref: '#/definitions/test/UserSelectOmitFields'
      description: Fields to omit
    include:
      $ref: '#/definitions/test/UserRelations'
  required:
    - data
  description: Update many and return User args
UserUpdateManyAndReturnOutput:
  type: array
  items:
    $ref: '#/definitions/test/User'
  description: Updated User array
