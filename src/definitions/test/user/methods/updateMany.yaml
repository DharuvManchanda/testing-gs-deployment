UserUpdateManyArgs:
  type: object
  properties:
    data:
      oneOf:
        - $ref: '#/definitions/test/UserUpdateInput'
        - $ref: '#/definitions/test/UserUpdateInputUnchecked'
    where:
      $ref: '#/definitions/test/UserWhereInput'
      description: Filter for update
  required:
    - data
  description: Update many User args
UserUpdateManyOutput:
  type: object
  properties:
    count:
      type: integer
  description: Update many User output
