PostFindManyArgs:
  type: object
  properties:
    where:
      $ref: '#/definitions/test/PostWhereInput'
    orderBy:
      oneOf:
        - $ref: '#/definitions/test/PostOrderByWithRelationInput'
        - type: array
          items:
            $ref: '#/definitions/test/PostOrderByWithRelationInput'
    cursor:
      $ref: '#/definitions/test/PostWhereUniqueInput'
    take:
      type: integer
    skip:
      type: integer
    distinct:
      type: array
      items:
        $ref: '#/definitions/test/PostScalarFieldEnum'
    select:
      $ref: '#/definitions/test/PostSelectOmitFields'
      description: Fields to select
    omit:
      $ref: '#/definitions/test/PostSelectOmitFields'
      description: Fields to omit
    include:
      $ref: '#/definitions/test/PostRelations'
  description: Find many Post
PostFindManyOutput:
  type: array
  items:
    $ref: '#/definitions/test/Post'
  description: Post array
