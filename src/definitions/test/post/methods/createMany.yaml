PostCreateManyArgs:
  type: object
  properties:
    data:
      type: array
      items:
        oneOf:
          - $ref: '#/definitions/test/PostCreateInput'
          - $ref: '#/definitions/test/PostCreateInputUnchecked'
    skipDuplicates:
      type: boolean
  required:
    - data
  description: Create many Post args
PostCreateManyOutput:
  type: object
  properties:
    count:
      type: integer
  description: Create many Post output
