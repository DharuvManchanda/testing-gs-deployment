PostCountArgs:
  type: object
  properties:
    where:
      $ref: '#/definitions/test/PostWhereInput'
      description: Filter for count
    orderBy:
      oneOf:
        - $ref: '#/definitions/test/PostOrderByWithRelationInput'
        - type: array
          items:
            $ref: '#/definitions/test/PostOrderByWithRelationInput'
    cursor:
      $ref: '#/definitions/test/PostWhereUniqueInput'
    take:
      type: integer
    skip:
      type: integer
    distinct:
      type: array
      items:
        $ref: '#/definitions/test/PostScalarFieldEnum'
  description: Count Post args
PostCountOutput:
  type: integer
  description: Count of Post
