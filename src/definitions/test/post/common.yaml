PostWhereInput:
  type: object
  properties:
    id:
      type: object
      properties:
        equals:
          type: integer
        in:
          type: array
          items:
            type: integer
        gt:
          type: integer
        lt:
          type: integer
    title:
      type: object
      properties:
        equals:
          type: string
        in:
          type: array
          items:
            type: string
        contains:
          type: string
    content:
      type: object
      properties:
        equals:
          type: string
        in:
          type: array
          items:
            type: string
        contains:
          type: string
    published:
      type: object
      properties:
        equals:
          type: boolean
        in:
          type: array
          items:
            type: boolean
    author:
      type: object
      properties:
        is:
          $ref: '#/definitions/test/UserWhereInput'
    authorId:
      type: object
      properties:
        equals:
          type: integer
        in:
          type: array
          items:
            type: integer
        gt:
          type: integer
        lt:
          type: integer
PostWhereUniqueInput:
  type: object
  properties:
    id:
      type: integer
      description: Post unique identifier by id
  description: Post unique input
PostOrderByWithRelationInput:
  type: object
  properties:
    id:
      type: string
      enum:
        - asc
        - desc
    title:
      type: string
      enum:
        - asc
        - desc
    content:
      type: string
      enum:
        - asc
        - desc
    published:
      type: string
      enum:
        - asc
        - desc
    author:
      type: string
      enum:
        - asc
        - desc
    authorId:
      type: string
      enum:
        - asc
        - desc
PostScalarFieldEnum:
  type: string
  enum:
    - id
    - title
    - content
    - published
    - author
    - authorId
  description: Fields for Post
PostSelectOmitFields:
  type: object
  properties:
    id:
      type: boolean
      description: Whether to select the id field
    title:
      type: boolean
      description: Whether to select the title field
    content:
      type: boolean
      description: Whether to select the content field
    published:
      type: boolean
      description: Whether to select the published field
    author:
      type: boolean
      description: Whether to select the author field
    authorId:
      type: boolean
      description: Whether to select the authorId field
    _count:
      type: boolean
      description: Whether to include the count of records
  additionalProperties: true
PostRelations:
  type: object
  properties:
    author:
      type: object
      properties:
        model:
          type: string
          const: User
        relationType:
          type: string
          const: belongsTo
        required:
          type: boolean
          default: false
PostBatchPayload:
  type: object
  properties:
    count:
      type: integer
      description: The number of records affected by the operation
  required:
    - count
  description: Result of a batch operation
